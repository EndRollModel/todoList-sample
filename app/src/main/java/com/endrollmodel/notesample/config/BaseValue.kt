package com.endrollmodel.notesample.config

object BaseValue {
    // 元件參數
    const val mainTabBaseLine : Float = 0.06f // mainTab height
    const val tabHeightBaseLine : Float = 0.07f // todoList tab height
    const val todoItemCount : Int = 10 // todoList item count
    const val todoGridCount : Int = 2 // todoList GridView Count
}